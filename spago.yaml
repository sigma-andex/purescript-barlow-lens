package:
  dependencies:
    - either
    - foldable-traversable
    - lists
    - maybe
    - newtype
    - prelude
    - profunctor
    - profunctor-lenses
    - tuples
    - typelevel-prelude
  name: barlow-lens
  test:
    main: Test.Main
    # This works like `package.dependencies`
    dependencies:
      - aff
      - effect
      - spec
      - spec-discovery
workspace:
  extra_packages: {}
  package_set:
    url: https://raw.githubusercontent.com/purescript/package-sets/psc-0.15.0-20220513/packages.json
