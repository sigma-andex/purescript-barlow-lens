package:
  dependencies:
    - either
    - foldable-traversable
    - lists
    - maybe
    - newtype
    - prelude
    - profunctor
    - profunctor-lenses
    - tuples
    - typelevel-prelude
  name: barlow-lens
  test:
    main: Test.Main
    # This works like `package.dependencies`
    dependencies:
      - aff
      - effect
      - spec
      - spec-discovery
workspace:
  extra_packages: {}
  packageSet:
    registry: 77.4.0
